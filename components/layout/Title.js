import classes from './Title.module.css';

const Title = () => {

    return(
        <div className={classes.title}>
            Create ig Post for @CalcioNews
        </div>
    );
}

export default Title;